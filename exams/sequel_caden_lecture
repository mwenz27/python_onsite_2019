SQL
RDBMS

rules in databases,

no duplicates
primary key in a database


students - id, name, age, sex, status, addrese

cousrse - id name creat

sequal statment, query statment

INSERT INTO students (name, age) VALUES  ('daniel, 32')

SELECT s.name, c.name FROM students as s
JOIN students_course as sc
ON s.id = sc.student.id
JOIN courses as c
ON sc.course_id = c.id

WHERE s.id = :